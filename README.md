# rush | Custom UNIX Shell in Rust

A lightweight, POSIX compliant UNIX-like shell built from scratch in Rust. Its features include command-line parsing, redirection and appending, environment/shell variable expansion, background job processing, and tab-completions.